target-version = "py311"
line-length = 100

[lint]
select = ["E", "F", "W", "I", "UP", "B", "C4", "SIM", "ASYNC"]

exclude = [
    "__pycache__",
    ".git",
    ".venv",
    "build",
    "dist",
    ".eggs",
]
